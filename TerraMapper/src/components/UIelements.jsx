/* import { Children } from "react"

const UIelements =(props)=>{

    return (
        <div>
         <VisibilityContext.Provider visibilityCntx = {{show3D : show3D, ShowDownloads : ShowDownloads, showCreateMod : showCreateMod, showThreeDinfo : showThreeDinfo}}>
          <OrientationContext.Provider orientationCntx = {Orientation}> 
                {children}
              </OrientationContext.Provider>
          </VisibilityContext.Provider>

        </div>
    )
}
export default UIelements */